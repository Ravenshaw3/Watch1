# Local Development Environment Configuration
# Uses direct T: drive scanner to bypass Docker Desktop mount issues
# Accesses same Unraid media as production but via host-based scanning

# Media Mount Configuration (fallback only - uses direct scanner)
MEDIA_SOURCE=./media
MEDIA_TARGET=/app/media
MEDIA_MODE=rw

# Environment Type
ENV_TYPE=local
MEDIA_DESCRIPTION=Local development with direct T: drive scanner (18,509+ Unraid files)
UNRAID_ACCESS_METHOD=direct_scanner
T_DRIVE_PATH=T:\

# Database Configuration
DB_NAME=watch1_dev
DB_USER=watch1_user
DB_PASSWORD=watch1_dev_password

# Development Settings
FLASK_ENV=development
FLASK_DEBUG=true
NODE_ENV=development

# API Configuration
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production

# Logging
LOG_LEVEL=DEBUG
